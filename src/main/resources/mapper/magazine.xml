<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.model.mapper.MagazineMapper">
    <insert id="createMagazine" parameterType="MagazineDto">
        insert into magazine(magazine_id, user_id, travel_id, magazine_title)
        values (#{magazine_id}, #{user_id}, #{travel_id}, #{magazine_title})
    </insert>

    <select id="viewMagazine" parameterType="string" resultType="MagazineDto">
        select magazine_id, user_id, travel_id, magazine_title
        from magazine
        where magazine_id = #{magazine_id}
    </select>

    <select id="getMagazineIdList" parameterType="string" resultType="string">
        select magazine_id
        from magazine
        where user_id = #{user_id}
    </select>


</mapper>